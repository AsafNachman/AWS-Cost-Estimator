# --- Public env vars (must be NEXT_PUBLIC_-prefixed to reach the browser) ---
# Override the default backend URL if your FastAPI server runs somewhere else.
# Defaults to http://localhost:8000 when not set.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
