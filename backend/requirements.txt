# --- Web framework -----------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# --- Data validation ---------------------------------------------------------
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- LangChain / LangGraph ---------------------------------------------------
langgraph>=0.2.50
langchain>=0.3.7
langchain-core>=0.3.15
langchain-openai>=0.2.8
langchain-anthropic>=0.2.4

# --- Async HTTP for (future) real Infracost calls ---------------------------
httpx>=0.27.2

# --- Env / config ------------------------------------------------------------
python-dotenv>=1.0.1

# --- Optional: MongoDB persistence (bonus) -----------------------------------
motor>=3.6.0

# --- Resilience: retry with exponential backoff ------------------------------
# Used by the persistence layer to absorb transient Mongo socket / network
# errors without surfacing them as user-facing failures.
tenacity>=9.0.0
